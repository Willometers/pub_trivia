import HomeButton from "./HomeButton.js"
import MenuButton from "./MenuButton.js";

function NavBar() {

    return (
        <div>
         <h1>NavBar</h1> 
        < HomeButton />
        <br/>
        < MenuButton />
        </div>
    );
}

export default NavBar;